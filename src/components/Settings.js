import Popup from './Popup';

function Settings() {

    return(
        <div className="settings">
            <i className="fas fa-cog"></i>
            <Popup type='settings' />
        </div>
    );
}

export default Settings; 
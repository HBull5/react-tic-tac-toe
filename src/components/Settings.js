import Popup from './Popup';

function Settings() {
    return(
        <div class="settings">
            <i class="fas fa-cog"></i>
            <Popup type='settings' />
        </div>
    );
}

export default Settings; 